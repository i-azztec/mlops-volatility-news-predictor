apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: postgres_datasource
    access: proxy
    url: postgres:5432
    database: monitoring
    user: user
    secureJsonData:
      password: password
    jsonData:
      sslmode: disable
